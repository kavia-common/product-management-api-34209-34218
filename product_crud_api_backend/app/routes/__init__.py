"""Routes package for the Product CRUD API.

This module exposes blueprints through submodules like:
- health (service liveness)
- products (CRUD operations)
"""
# This file intentionally left minimal to mark the package.
